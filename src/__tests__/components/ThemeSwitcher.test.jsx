// ThemeContext.Provider with { currentTheme: "light", switchTheme: vi.fn() }

// Renders with current theme value selected (the Select's value is "light")

// Calls switchTheme when selection changes
// fire a change event on the Select with a new value
// assert the mock was called